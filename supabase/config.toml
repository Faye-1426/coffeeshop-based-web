# Replace with your project ID when using Supabase CLI (`supabase link`).
# Run migrations: `supabase db push` or paste SQL from `migrations/` into the SQL editor.
project_id = "qwynusbfegqauhcajvue"

[api]
enabled = true

# Customer checkout calls midtrans-snap with the anon JWT.
[functions.midtrans-snap]
verify_jwt = false

# Midtrans HTTP notifications have no Supabase JWT.
[functions.midtrans-webhook]
verify_jwt = false
